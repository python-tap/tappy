repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.11.2
  hooks:
    # Run the linter.
    - id: ruff
      args: [ --fix, --unsafe-fixes ]
    # Run the formatter.
    - id: ruff-format
